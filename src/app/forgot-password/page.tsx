'use client';import{FormEvent,useState}from'react';import Link from'next/link';import{sendPasswordResetEmail}from'firebase/auth';import{firebaseAuth}from'@/lib/firebaseClient';export default function Page(){const[email,setEmail]=useState(''),[message,setMessage]=useState(''),[saving,setSaving]=useState(false);async function submit(e:FormEvent){e.preventDefault();setSaving(true);try{if(!firebaseAuth)throw new Error('Firebase Authentication is not configured.');await sendPasswordResetEmail(firebaseAuth,email,{url:`${location.origin}/sign-in`});setMessage('Check your email for a secure password reset link.')}catch(error:any){setMessage(error.message)}setSaving(false)}return <div className="mx-auto flex min-h-[70vh] max-w-md items-center"><form onSubmit={submit} className="w-full rounded-[2rem] border border-[var(--afrigo-border)] bg-white p-8 shadow-xl"><p className="text-sm font-semibold uppercase tracking-widest text-[var(--afrigo-secondary-gold)]">Account recovery</p><h1 className="mt-3 text-3xl font-black text-[var(--afrigo-primary-green)]">Reset your password</h1><p className="mt-3 text-sm text-[var(--afrigo-text-secondary)]">Firebase will send a time-limited recovery link to your email.</p><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com" className="mt-6 w-full rounded-2xl border border-[var(--afrigo-border)] px-4 py-3"/><button disabled={saving} className="mt-4 w-full rounded-2xl bg-[var(--afrigo-primary-green)] px-5 py-3 font-semibold text-white disabled:opacity-60">{saving?'Sending…':'Send recovery link'}</button>{message&&<p role="status" className="mt-4 text-sm text-[var(--afrigo-text-secondary)]">{message}</p>}<Link href="/sign-in" className="mt-6 block text-center text-sm font-semibold text-[var(--afrigo-primary-green)]">Back to sign in</Link></form></div>}
