import{requireUser,jsonError}from'@/lib/serverAuth';import{rateLimit}from'@/lib/rateLimit';export async function POST(request:Request){try{rateLimit(request,'payment',10);const{user,admin}=await requireUser(request),body=await request.json(),secret=process.env.PAYSTACK_SECRET_KEY;if(!secret)return Response.json({ok:false,error:'Payment provider is not configured'},{status:503});const amount=Number(body.amount);if(!body.contractId||!Number.isFinite(amount)||amount<=0)return Response.json({ok:false,error:'Valid contract and amount are required'},{status:400});const reference=`afrigo_${crypto.randomUUID().replace(/-/g,'')}`,response=await fetch('https://api.paystack.co/transaction/initialize',{method:'POST',headers:{Authorization:`Bearer ${secret}`,'Content-Type':'application/json'},body:JSON.stringify({email:user.email,amount:String(Math.round(amount*100)),currency:body.currency||'NGN',reference,callback_url:`${process.env.NEXT_PUBLIC_SITE_URL}/dashboard`,metadata:JSON.stringify({contractId:body.contractId,userId:user.id})})}),result=await response.json();if(!response.ok||!result.status)throw new Error(result.message||'Unable to initialize payment');await admin.db.collection('payments').add({contractId:body.contractId,payerId:user.id,provider:'paystack',providerReference:reference,amount,currency:body.currency||'NGN',status:'pending',createdAt:new Date()});return Response.json({ok:true,authorizationUrl:result.data.authorization_url,reference})}catch(error){return jsonError(error)}}
