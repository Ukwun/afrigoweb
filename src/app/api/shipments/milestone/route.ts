import { jsonError, requireUser } from '@/lib/serverAuth'
import { sendTransactionalEmail } from '@/lib/transactionalEmail'
import { rateLimit } from '@/lib/rateLimit'

const milestones:Record<string,number>={Assigned:5,'Picked Up':20,'In Transit':45,'Customs Clearance':70,Cleared:85,Delivered:100}
export async function POST(request:Request){try{rateLimit(request,'shipment-milestone',30);const {user,admin}=await requireUser(request),input=await request.json(),ref=admin.db.collection('shipments').doc(String(input.shipmentId||'')),shipment=await ref.get(),data=shipment.data(),status=String(input.status||'');if(!shipment.exists||data?.exporterId!==user.id)return Response.json({ok:false,error:'Shipment not found'},{status:404});if(!(status in milestones))return Response.json({ok:false,error:'Invalid shipment milestone'},{status:400});await ref.update({status,progress:milestones[status],trackingNumber:String(input.trackingNumber||data?.trackingNumber||'').slice(0,100),carrier:String(input.carrier||data?.carrier||'DHL').slice(0,100),updatedAt:new Date()});const profiles=await Promise.all([admin.db.collection('users').doc(data.buyerId).get(),admin.db.collection('users').doc(data.supplierId).get()]),emails=profiles.map(profile=>String(profile.data()?.email||'')).filter(Boolean),notice=await sendTransactionalEmail(emails,`Shipment ${shipment.id.slice(0,8)}: ${status}`,`<p>Your Afrigo shipment is now <strong>${status}</strong>.</p>`);await admin.db.collection('activityLogs').add({actorId:user.id,type:'shipment_update',label:`Shipment ${status}`,detail:shipment.id,role:'Exporter',notificationSent:notice.sent,createdAt:new Date()});return Response.json({ok:true,status,notificationSent:notice.sent})}catch(error){return jsonError(error)}}
